<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rec_view_parent"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="5dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="10dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp">

        <TextView
            android:id="@+id/food_item_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Name"
            android:maxLength="8"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/food_item_expiry_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="70dp"
            android:layout_toStartOf="@+id/rec_view_parent"
            android:text="Date"
            android:textSize="15sp"
            tools:ignore="NotSibling" />

        <TextView
            android:id="@+id/food_item_quantity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_toEndOf="@+id/food_item_expiry_date"
            android:text="Quantity"
            android:textSize="15sp"
            tools:ignore="NotSibling" />

        <Button
            android:id="@+id/delete_button"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:layout_alignParentEnd="true"
            android:layout_marginEnd="5dp"
            android:background="@drawable/delete_button"
            android:text="@string/delete_button_sign"
            android:textColor="#ffffff"
            android:textSize="15sp" />

    </RelativeLayout>

</androidx.cardview.widget.CardView>